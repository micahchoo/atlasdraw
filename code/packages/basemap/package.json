{
  "name": "@atlasdraw/basemap",
  "version": "0.0.0",
  "description": "MapLibre wrapper: MapCanvas + style compiler (LayerStyle, compileLayer). BasemapRegistry + pmtiles protocol deferred to Phase 4 (self-host).",
  "license": "MPL-2.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "echo 'TODO: build (target: Phase 1+)' && exit 0",
    "build:styles": "node scripts/build-styles.mjs",
    "test": "vitest",
    "test:typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@atlasdraw/geo": "0.1.0",
    "maplibre-gl": "^4.7.1",
    "pmtiles": "^4.4.0"
  },
  "peerDependencies": {
    "react": ">=18.0.0"
  },
  "devDependencies": {
    "protomaps-themes-base": "^4.5.0",
    "typescript": "5.9.3"
  }
}
