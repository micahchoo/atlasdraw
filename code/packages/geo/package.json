{
  "name": "@atlasdraw/geo",
  "version": "0.1.0",
  "description": "Atlasdraw geo math: GeoAnchor types, projection, CoordinateSync, Turf wrappers — pure functions",
  "license": "MIT",
  "private": false,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "echo 'TODO: build (target: Phase 1+)' && exit 0",
    "test": "vitest run",
    "test:typecheck": "tsc --noEmit",
    "bench": "vitest run --config bench/vitest.config.ts"
  },
  "dependencies": {},
  "peerDependencies": {
    "maplibre-gl": ">=4.0.0"
  },
  "peerDependenciesMeta": {
    "maplibre-gl": {
      "optional": true
    }
  },
  "devDependencies": {
    "fast-check": "4.7.0",
    "maplibre-gl": "^4.7.1",
    "typescript": "*",
    "vitest": "*"
  }
}
