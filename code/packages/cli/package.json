{
  "name": "@atlasdraw/cli",
  "version": "0.0.0",
  "description": "Headless tooling: lint, convert, render",
  "license": "MIT",
  "private": false,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "bin": {
    "atlasdraw": "src/index.ts"
  },
  "files": ["src", "README.md"],
  "scripts": {
    "build": "echo 'TODO: build (target: Phase 3 T10/T11)' && exit 0",
    "test": "vitest run",
    "test:typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@atlasdraw/data": "0.0.0",
    "commander": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "*",
    "typescript": "*",
    "vitest": "3.0.6"
  }
}
