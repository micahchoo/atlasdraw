{
  "name": "@atlasdraw/bench",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Phase 2 benchmark harness — pure parse/validate timings, no MapLibre runtime.",
  "scripts": {
    "bench": "vitest run --config ./vitest.config.ts",
    "ci-gate": "tsx ci-gate.ts",
    "test:typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@atlasdraw/data": "*",
    "@atlasdraw/geo": "*"
  },
  "devDependencies": {
    "tsx": "^4.0.0"
  }
}
