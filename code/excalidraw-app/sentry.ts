// stripped: Sentry init (ADR 0006) — entire module neutered in Phase 0 Task 9b.
// Original file wired @sentry/browser with Sentry.init(), captureConsoleIntegration,
// featureFlagsIntegration, and beforeSend. All removed.
// Re-evaluate in Phase 5 if structured error reporting is required.
export {};
