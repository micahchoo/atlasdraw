# Dependencies
node_modules/
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

# Build outputs
build/
dist/
*.tgz
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local
.envrc

# Logs
*.log
logs/

# Editor / OS
.DS_Store
.idea/
.vscode/
.history/
.eslintcache

# Search index (rebuilt from source)
.foxhound/

# Ephemeral runtime logs
.claude/.skill-invocation-log

# Auto-generated diagnostics (regenerable)
.claude/SUGGESTED_SKILLS.md
anti-pattern-report.txt

# Firebase build artifacts
firebase/
.vercel/

# playwright-cli session artifacts
.playwright-cli/
.playwright/

# Yarn install temp unpack dirs (created by `yarn install` at repo root,
# abandoned across runs — never tracked)
yarn--*/

# Build / process caches at repo root
playwright-transform-cache-*/
v8-compile-cache-*/
node-compile-cache/

# Stray tool artifact dir (content-addressed 'web/' store; not ours)
Et6ZJ_xdtT9_0ghOD3LnM/

# Autoresearch session artifacts (machine-local; regenerated per loop)
autoresearch-metrics.json
hypotheses.jsonl

# deps-index runtime data (pending/resolved JSON; the rule.md is tracked)
.claude/.deps-index/
