name: Bench CI gate

on:
  pull_request:
    branches:
      - main

jobs:
  bench:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Must precede setup-node: its `cache: yarn` step shells out to yarn to
      # locate the cache folder, and classic yarn 1.22 errors on the
      # `packageManager: yarn@4` pin unless corepack has already shimmed yarn.
      - name: Enable corepack
        run: corepack enable

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "yarn"
          cache-dependency-path: code/yarn.lock

      - name: Install dependencies
        run: yarn install --immutable
        working-directory: code

      - name: Run Phase 2 bench scenarios
        run: yarn workspace @atlasdraw/bench bench
        working-directory: code

      - name: Assert regression gate
        run: yarn workspace @atlasdraw/bench ci-gate
        working-directory: code
